services:
  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build: .
    ports:
      - "${DOCINTEL_PORT:-5000}:5000"
    environment:
      DOCINTEL_HOST: 0.0.0.0
      DOCINTEL_PORT: 5000
      DOCINTEL_LOG_LEVEL: ${DOCINTEL_LOG_LEVEL:-INFO}
      DOCINTEL_REDIS_URL: redis://redis:6379/0
      WEB_CONCURRENCY: ${WEB_CONCURRENCY:-1}
    volumes:
      - docintel_uploads:/app/uploads
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5000/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s

  worker:
    build: .
    environment:
      DOCINTEL_LOG_LEVEL: ${DOCINTEL_LOG_LEVEL:-INFO}
      DOCINTEL_REDIS_URL: redis://redis:6379/0
      DOCINTEL_UPLOAD_DIR: /app/uploads
    volumes:
      - docintel_uploads:/app/uploads
    depends_on:
      redis:
        condition: service_healthy
      api:
        condition: service_healthy
    command: ["python", "run_worker.py"]

  ui:
    build: .
    ports:
      - "${GRADIO_PORT:-7860}:7860"
    environment:
      DOCINTEL_API_URL: http://api:5000
      GRADIO_SERVER_NAME: 0.0.0.0
      GRADIO_SERVER_PORT: 7860
    depends_on:
      api:
        condition: service_healthy
    command: ["python", "run_ui.py"]

volumes:
  docintel_uploads:
