# Build the shared dependency base image (spaCy, Presidio, PyTorch, EasyOCR).
# Run once or when pyproject.toml extras change: make docker-build-base

services:
  platform-base:
    build:
      context: .
      dockerfile: Dockerfile
      target: platform-base
    image: docintel-platform-base:3.11
